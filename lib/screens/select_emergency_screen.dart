

import 'package:flutter/material.dart';

class SelectEmergencyScreen extends StatelessWidget {
  const SelectEmergencyScreen({
    super.key,
  });
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
        body: Center(
            child: Text("SelectEmergency Screen" ,style: TextStyle(fontSize:40 ,color: Colors.red),)));
  }
}

